import 'package:flutter/material.dart';

class ForPartners extends StatelessWidget {
  const ForPartners({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('FOR PARTNERS'),
      ),
    );
  }
}
